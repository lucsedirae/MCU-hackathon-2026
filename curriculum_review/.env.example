# Local development only. Copy to .env to override these defaults.
POSTGRES_USER=curriculum_review
POSTGRES_PASSWORD=local_dev_password
POSTGRES_DB=curriculum_review
FRONTEND_PORT=5173
BACKEND_PORT=8000

# Set true when serving the app through an HTTPS reverse proxy.
COOKIE_SECURE=false
